{
  "name": "jobconnect",
  "version": "1.0.0",
  "description": "A job board platform connecting employers and job seekers",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["job", "board", "mern", "react", "node", "mongodb"],
  "author": "JobConnect Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
